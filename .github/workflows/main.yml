name: Compile and deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    # Make sure the @v0.9.0 matches the current version of the action
    - uses: webfactory/ssh-agent@v0.9.0
      with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Backup current version
      run: |
        ssh ${{ secrets.USERNAME }}@${{ secrets.SERVER_IP }} << 'EOF'
          cp -r /var/www/html/testcompang ~/testcompang-$(date +%F-%T)
        EOF

    - name: Build the project
      run: |
        npm install
        ng build --configuration=production

    - name: Deploy to server
      run: |
        rsync -avz --delete-after ./dist/ ${{ secrets.USERNAME }}@${{ secrets.SERVER_IP }}:/var/www/html/testcompang/
